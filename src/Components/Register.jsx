import React from "react";
import FormLS from "./FormLS.jsx";
const Register = () => {
  return (
    <FormLS heading={"Sign Up"} link={"login"} message={"already a user ? "} />
  );
};

export default Register;
